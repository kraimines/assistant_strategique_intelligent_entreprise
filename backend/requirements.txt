fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
pydantic==2.10.3
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
bcrypt==4.0.1
python-multipart==0.0.20
psycopg2-binary==2.9.10
asyncpg==0.30.0
langchain==0.3.9
langgraph==0.2.56
langchain-google-genai>=2.1.0
langchain-groq>=0.2.0,<0.3.0
langchain-mistralai>=0.2.0
chromadb==0.5.20
sentence-transformers==3.3.1
langchain-community>=0.3.0
rank-bm25>=0.2.2
pypdf>=4.0.0
docx2txt>=0.8
pandas==2.2.3
openpyxl==3.1.5
py2neo==2021.2.4
redis==5.2.0
numpy==1.26.4
# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0
anyio[trio]==4.6.2
httpx==0.28.1
# ── Neo4j official async driver ───────────────────────────────────────────────
neo4j==5.26.0
# ── Scheduler ─────────────────────────────────────────────────────────────────
apscheduler==3.10.4
# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
email-validator==2.3.0
# ── Competitive Intelligence (web scraping) ───────────────────────────────────
feedparser==6.0.11
beautifulsoup4==4.12.3
lxml==5.3.0
# ── Market Analysis Agent ─────────────────────────────────────────────────────
yfinance>=0.2.36                    # price & news data from Yahoo Finance
httpx>=0.27.0                       # async HTTP client for NewsAPI / GNews
anthropic>=0.40.0                   # Claude API — structured tool-use extraction (optional)
# ── GNN Dataset Builder + Embedding Trainer ───────────────────────────────────
# sentence-transformers is already listed above (used for LLM embeddings).
networkx>=3.3                       # graph construction for GNN pipeline
node2vec>=0.4.6                     # structural random-walk embeddings
scikit-learn>=1.3.0                 # PCA fallback when torch absent
#
# PyTorch (GraphSAGE training) — install separately (heavy binary):
#   CPU:  pip install torch==2.3.0
#   CUDA: pip install torch==2.3.0+cu121 -f https://download.pytorch.org/whl/cu121
# embedding_trainer.py falls back to PCA (scikit-learn) when torch is absent.
# torch>=2.1.0                      # uncomment for GraphSAGE GPU training
# torch-geometric>=2.5.0            # uncomment for full PyG GNN pipeline
